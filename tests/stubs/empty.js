// Stub for the `server-only` Next.js convention. Re-exported as an empty
// module so vitest can resolve imports of `server-only` without running the
// real Next.js guard (which throws outside a Server Component module).
export {};
